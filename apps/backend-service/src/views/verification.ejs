<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <title><%= title %> · Postmatic</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <style>
      :root {
        --primary: oklch(51.59% 0.225 262.83);
        --primary-foreground: oklch(0.4911 0.225 262.83);
        --secondary: oklch(0.585 0.233 277.117);
        --secondary-foreground: oklch(0.5 0.233 277.117);
      }
      .bg-primary {
        background-color: var(--primary);
      }
      .bg-secondary {
        background-color: var(--secondary);
      }
      .bg-brand-gradient {
        background: linear-gradient(135deg, var(--primary), var(--secondary));
      }
      .ring-brand:focus {
        outline: none;
        box-shadow: 0 0 0 3px color-mix(in oklab, var(--primary) 60%, #ffffff);
      }
    </style>
  </head>

  <body class="min-h-screen bg-gray-50">
    <div class="grid min-h-screen lg:grid-cols-2">
      <!-- Panel Kiri (Brand) -->
      <aside
        class="hidden lg:flex flex-col justify-between p-10 bg-brand-gradient text-white"
      >
        <div>
          <div class="flex items-center gap-3">
            <div
              class="h-12 w-12 rounded-2xl bg-white/15 backdrop-blur-sm flex items-center justify-center overflow-hidden"
            >
              <img
                src="/static/logo.png"
                alt="Postmatic"
                class="h-full w-full object-cover"
              />
            </div>
            <div class="text-2xl font-semibold tracking-tight">Postmatic</div>
          </div>

          <h1 class="mt-16 text-4xl font-semibold max-w-lg"><%= title %></h1>
          <p class="mt-4 text-white/90 max-w-xl">
            Verifikasi email Anda untuk melanjutkan.
          </p>

          <div class="mt-10 grid grid-cols-2 gap-4 max-w-xl">
            <div class="rounded-2xl bg-white/10 p-5">
              <p class="text-sm font-medium">Status</p>
              <p class="text-white/80 text-xs mt-1">
                <%= success ? 'Berhasil' : 'Gagal' %>
              </p>
            </div>
            <div class="rounded-2xl bg-white/10 p-5">
              <p class="text-sm font-medium">Email</p>
              <p class="text-white/80 text-xs mt-1"><%= email || '-' %></p>
            </div>
          </div>
        </div>

        <div class="text-white/80 text-sm">
          &copy; <%= new Date().getFullYear() %> Postmatic. Hak cipta dilindungi
          undang-undang.
        </div>
      </aside>

      <!-- Panel Kanan (Konten Status) -->
      <main class="flex items-center justify-center p-6 sm:p-10">
        <div class="w-full max-w-md">
          <div class="bg-white shadow-xl rounded-2xl p-8 text-center">
            <% if (success) { %>
            <div
              class="mx-auto h-12 w-12 rounded-2xl bg-green-500 flex items-center justify-center"
            >
              <svg
                class="w-6 h-6 text-white"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  d="M5 13l4 4L19 7"
                />
              </svg>
            </div>
            <% } else { %>
            <div
              class="mx-auto h-12 w-12 rounded-2xl bg-red-500 flex items-center justify-center"
            >
              <svg
                class="w-6 h-6 text-white"
                viewBox="0 0 24 24"
                fill="none"
                stroke="currentColor"
                stroke-width="2"
              >
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  d="M6 18L18 6M6 6l12 12"
                />
              </svg>
            </div>
            <% } %>

            <h2 class="mt-5 text-2xl font-semibold text-gray-900">
              <%= title %>
            </h2>
            <p class="mt-2 text-sm text-gray-600"><%= message %></p>

            <% if (email) { %>
            <p class="mt-4 text-gray-500 text-sm">
              Email: <span class="font-medium text-gray-800"><%= email %></span>
            </p>
            <% } %> <% if (redirect && cta) { %>
            <a
              href="<%= redirect %>"
              class="mt-6 inline-flex w-full items-center justify-center rounded-xl bg-brand-gradient py-2.5 text-sm font-medium text-white shadow hover:opacity-95 transition ring-brand"
            >
              <%= cta %>
            </a>
            <% } %>
          </div>

          <!-- Footer kebijakan dengan LANDINGPAGE_URL prefix -->
          <footer class="mt-6 text-center text-xs text-gray-500">
            <a
              class="hover:underline"
              href="<%= LANDINGPAGE_URL %>/cookie-policy"
              >Kebijakan Cookie</a
            >
            ·
            <a
              class="hover:underline"
              href="<%= LANDINGPAGE_URL %>/data-deletion"
              >Penghapusan Data</a
            >
            ·
            <a
              class="hover:underline"
              href="<%= LANDINGPAGE_URL %>/privacy-policy"
              >Kebijakan Privasi</a
            >
            ·
            <a
              class="hover:underline"
              href="<%= LANDINGPAGE_URL %>/terms-of-service"
              >Syarat Layanan</a
            >
          </footer>
        </div>
      </main>
    </div>
  </body>
</html>
