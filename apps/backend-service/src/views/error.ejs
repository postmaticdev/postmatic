<!DOCTYPE html>
<html lang="id">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    
    <title><%= typeof code !== 'undefined' && code ? code : 500 %> · <%= typeof title !== 'undefined' && title ? title : 'Terjadi Kesalahan' %> · Postmatic</title>

    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="manifest" href="/site.webmanifest" />

    <style>
      :root {
        --primary: #1657e6; 
        --secondary: #4d41e3;
      }
      .bg-brand-gradient {
        background: linear-gradient(135deg, var(--primary), var(--secondary));
      }
      .ring-brand:focus {
        outline: none;
        box-shadow: 0 0 0 3px rgba(107, 92, 255, 0.35);
      }
    </style>
  </head>

  <body class="min-h-screen bg-gray-50">
    <div class="grid min-h-screen lg:grid-cols-2">
      <!-- Panel Kiri: Branding -->
      <aside
        class="hidden lg:flex flex-col justify-between p-10 bg-brand-gradient text-white"
      >
        <div>
          <div class="flex items-center gap-3">
            <div
              class="h-12 w-12 rounded-2xl bg-brand-gradient backdrop-blur-sm flex items-center justify-center overflow-hidden"
            >
              <img
                src="/static/logo.png"
                alt="Postmatic"
                class="h-full w-full object-cover"
              />
            </div>
            <div class="text-2xl font-semibold tracking-tight">Postmatic</div>
          </div>

          <h1 class="mt-12 text-4xl font-semibold max-w-lg"><%= typeof title !== 'undefined' && title ? title : 'Terjadi Kesalahan' %></h1>
          <p class="mt-4 text-white/90 max-w-xl"><%= typeof description !== 'undefined' && description ? description : 'Maaf, terjadi kesalahan pada sistem. Silakan coba lagi beberapa saat atau kembali ke halaman utama.' %></p>

          <div class="mt-8">
            <img
              src="/static/error.png"
              alt="Ilustrasi kesalahan"
              class="w-full max-w-md"
              loading="lazy"
            />
          </div>
        </div>

        <div class="text-white/80 text-sm">
          &copy; <%= new Date().getFullYear() %> Postmatic. Hak cipta dilindungi
          undang-undang.
        </div>
      </aside>

      <!-- Panel Kanan: Kartu Error -->
      <main class="flex items-center justify-center p-6 sm:p-10">
        <div class="w-full max-w-md">
          <div class="bg-white shadow-xl rounded-2xl p-8 text-center">
            <!-- Logo kecil -->
            <div
              class="mx-auto h-12 w-12 rounded-2xl bg-brand-gradient flex items-center justify-center overflow-hidden"
            >
              <img
                src="/static/logo.png"
                alt="Postmatic"
                class="h-full w-full object-cover"
              />
            </div>

            <!-- Kode error -->
            <div class="mt-4 inline-flex items-center gap-2">
              <span
                class="rounded-full bg-gray-100 text-gray-700 text-xs font-medium px-3 py-1 border border-gray-200"
              >
                Kode: <%= typeof code !== 'undefined' && code ? code : 500 %>
              </span>
            </div>

            <!-- Judul & deskripsi -->
            <h2 class="mt-4 text-2xl font-semibold text-gray-900">
              <%= typeof title !== 'undefined' && title ? title : 'Terjadi Kesalahan' %>
            </h2>
            <p class="mt-2 text-sm text-gray-600"><%= typeof description !== 'undefined' && description ? description : 'Maaf, terjadi kesalahan pada sistem. Silakan coba lagi beberapa saat atau kembali ke halaman utama.' %></p>

            <!-- Gambar error (mobile) -->
            <img
              src="/static/error.png"
              alt="Ilustrasi kesalahan"
              class="mt-6 w-full max-w-xs mx-auto rounded-xl border border-gray-200 shadow-sm lg:hidden"
              loading="lazy"
            />

            <!-- CTA -->
            <a
              href="<%= typeof ctaHref !== 'undefined' && ctaHref ? ctaHref : LANDINGPAGE_URL %>"
              class="mt-7 inline-flex w-full items-center justify-center rounded-xl bg-brand-gradient py-2.5 text-sm font-medium text-white shadow hover:opacity-95 transition ring-brand"
            >
              <%= typeof ctaText !== 'undefined' && ctaText ? ctaText : 'Kembali ke Halaman Utama' %>
            </a>

            <p class="mt-5 text-xs text-gray-500">
              Jika masalah berlanjut, hubungi dukungan kami.
            </p>
          </div>

          <!-- Footer Kebijakan -->
          <footer class="mt-6 text-center text-xs text-gray-500">
            <a class="hover:underline" href="<%= LANDINGPAGE_URL %>/cookie-policy"
              >Kebijakan Cookie</a
            >
            ·
            <a class="hover:underline" href="<%= LANDINGPAGE_URL %>/data-deletion"
              >Penghapusan Data</a
            >
            ·
            <a class="hover:underline" href="<%= LANDINGPAGE_URL %>/privacy-policy"
              >Kebijakan Privasi</a
            >
            ·
            <a class="hover:underline" href="<%= LANDINGPAGE_URL %>/terms-of-service"
              >Syarat Layanan</a
            >
          </footer>
        </div>
      </main>
    </div>
  </body>
</html>
